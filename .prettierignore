# index.html is generated by Eleventy (npm run build) and committed by the
# build workflow. Formatting it by hand puts the local copy and the CI copy
# permanently out of step: prettier makes it 296KB, Eleventy writes 239KB,
# and each run reverts the other.
index.html
admin/index.html
sitemap.xml
_site/
