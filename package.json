{
  "name": "rosehallgren.se",
  "version": "1.0.0",
  "private": true,
  "description": "rosehallgren.se \u2014 vanilla site, Pages CMS content in-repo, pre-rendered with Eleventy into index.html",
  "scripts": {
    "build": "npm run assets && npm run validate && eleventy && node scripts/publish.js",
    "serve": "eleventy --serve",
    "validate": "node scripts/validate-content.js",
    "clean": "rm -rf _site",
    "css": "node scripts/build-css.js",
    "js": "node scripts/build-js.js",
    "assets": "node scripts/build-css.js && node scripts/build-js.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.2.0"
  }
}
